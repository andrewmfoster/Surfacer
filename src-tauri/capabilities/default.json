{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main board window. The tray and overlay windows are scoped separately (tray.json / overlay.json) for least-privilege built-in permissions instead of inheriting core:default.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging"
  ]
}
